# MongoDB - MONGO_URL must be set in production (e.g. Railway, Render, Heroku)
spring.data.mongodb.uri=${MONGO_URL:mongodb://localhost:27017/demo}

# Use PORT from environment on deployment (Railway, Render, Heroku, etc.)
server.port=${PORT:8080}
